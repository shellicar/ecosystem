// Core service types come from the shared engine.
export type { AbstractNewable, InstanceFactory, Newable, ResolvedDeps, ServiceIdentifier, ServiceImplementation, ServiceRegistration, SourceType, ValidationProblem, ValidationReport } from '@shellicar/core-di-engine';
