/**
 * Installs the `Symbol.metadata` polyfill `@dependsOn` needs. The install itself
 * moved to @shellicar/core-di-engine; this subpath stays for explicit control.
 */
import '@shellicar/core-di-engine/polyfill';

export {};
