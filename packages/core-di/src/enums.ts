// Moved to @shellicar/core-di-engine; re-exported so core-di's public surface and
// internal imports are unchanged and enum identity is shared across both packages.
export { CaptivePolicy, Lifetime, LogLevel, ResolveMultipleMode, RuntimeCaptivePolicy, ValidationProblemKind } from '@shellicar/core-di-engine';
