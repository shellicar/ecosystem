// Moved to @shellicar/core-di-engine; shim keeps core-di's internal import paths stable.
export { buildPlan, concreteNode, deriveFacts, detectCycles, findUnregisteredEdges, followForward, formatGraph, indexByOwner, type OwnerIndex, type Plan, type PlanStep, reachableFrom, topologicalOrder } from '@shellicar/core-di-engine';
