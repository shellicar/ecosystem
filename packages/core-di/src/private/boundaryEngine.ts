// Moved to @shellicar/core-di-engine; shim keeps core-di's internal import paths stable.
export { type Boundary, type BuildEngineOptions, buildEngine, buildEngineAsync, createNaiveStrategy, createPlanStrategy, type DisposalSink, type Engine, type EngineComposition, type EngineFor, type Scope, type ScopeOverlay } from '@shellicar/core-di-engine';
