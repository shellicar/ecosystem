{"description":"Initial release: the static-DAG engine that core-di and core-di-lite compose from, extracted so both packages share one resolution engine.","category":"added"}
{"description":"Lifetimes are composable features (singleton, scoped, resolve); a composition simply lacks the verb for a feature it doesn't include. Transient is the floor: no feature caches it, so it is the uncached default.","category":"added"}
{"description":"`runGraphPolicies` and the named policy functions (`missingTargetPolicy`, `cyclePolicy`, `captivePolicyFor`, `asyncThroughSyncPathPolicy`) are the composable primitives a consumer wires into its own `validate()`, producing a `ValidationReport` of `ValidationProblem` entries with no construction.","category":"added"}
{"description":"Disposal is a composed feature: each disposable is tracked to the boundary that resolved it and torn down there, not only at the root.","category":"added"}
{"description":"Async factories (`usingAsync`) build at `buildEngineAsync`, the async build boundary, which awaits async singletons in dependency order; `resolve()` stays synchronous.","category":"added"}
{"description":"`.eager()` constructs a singleton at build instead of on first resolve.","category":"added"}
{"description":"`printGraph(write)` writes a human-readable dependency graph: registered tokens, their dependency edges, and effective lifetimes.","category":"added"}
{"description":"`CaptivePolicy` (build-time, checked by `validate()`) and `RuntimeCaptivePolicy` (resolve-time, checked by `resolve()`) configure how a singleton reaching a shorter-lived dependency is reported or enforced.","category":"added"}
{"description":"Error types: `ServiceError`, `BuilderError`, `UnregisteredServiceError`, `MultipleRegistrationError`, `ServiceCreationError`, `SelfDependencyError`, `CircularDependencyError`, `ScopedSingletonRegistrationError`, `InvalidServiceIdentifierError`, `InvalidImplementationError`, `ValidationError`, `InvalidOperationError`, `CaptiveDependencyError`.","category":"added"}
{"type":"release","version":"5.0.0","date":"2026-07-16","tag":"core-di-engine@5.0.0","description":"The shared engine that core-di and core-di-lite compose from: the static-DAG build/resolve model, composable lifetimes, graph-policy validation, and boundary-scoped disposal, extracted so both packages share a single engine copy."}
