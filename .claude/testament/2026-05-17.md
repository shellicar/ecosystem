# 03:35

## Phase 1 — Scaffolder (whereOr Red)

Added a `describe('whereOr', ...)` block at the end of `packages/cosmos-query-builder/test/operations.spec.ts` covering all four bug paths in `whereOr` and the compound counter-collision case. Tests follow the existing file style: nested `describe` per operator, one assertion per `it`, `expect(query.query).toBe(...)` with template-literal expected strings.

## What the Red state actually looks like

11 new tests; 9 fail, 2 pass. The two passers are intentional, not a mistake:

- `in > emits ARRAY_CONTAINS wrapped in the OR group` — passes against current code
- `contains > emits ARRAY_CONTAINS wrapped in the OR group` — passes against current code

The brief calls these "the operator is declared in the type, but the runtime path does not carry the parameter through to `_parameters`." For a single condition, the clause text IS emitted correctly; only the parameter push is missing. So the query-text test passes; the companion `parameters[0]` test fails. The bug surfaces fully in the compound case (counter never advanced ⇒ collision on `@p0`), which is where the `counter advancement` tests bite.

For `ieq`/`ine`, no clause is emitted at all (the fallback `else` requires `operators[key] != null`, which is false for these). The query is literally `SELECT * FROM c` with no WHERE. Both the query-text and parameter tests fail.

So the failure pattern matches the brief: 4 bugs × {query-text or parameter assertion} = 8 directly-attributable failures, plus 3 compound/mixed failures from counter non-advancement. Two of the "8" happen to be already-green because the clause text alone is what's emitted on the happy single-condition path.

## Decisions inside the brief's gaps

- **Field choice**: brief used `c.field` as a placeholder. `whereOr`'s signature takes `field: string` (not a path expression), but I used `givenName`/`familyName` from `MyEntity` to stay consistent with the surrounding file rather than introducing arbitrary strings.

- **Single-assertion form for `parameters[0]`**: the brief lists each bullet as one `it`. The parameter-binding bullets ("`parameters[0]` is bound to the value with name `@p0`") cover both name and value. I asserted via `toEqual({ name: '@p0', value: ... })` — one structural assertion per `it`, which keeps TDD's one-assertion rule intact while honouring the brief's single-bullet-per-test count.

- **Mixed-operator test choice of operators**: brief said "e.g. `in`, `eq`, `contains`, `ieq`". Used exactly those four, in that order, so the parameter indices `@p0..@p3` line up one-per-operator and the expected query string is unambiguous.

## What the Builder needs

The fix mirrors `where` (singular). The `where` reference branches for `'in'`, `'contains'`, `'ieq'`, `'ine'` are at lines ~190–225 of `CosmosQueryBuilder.ts`. Each pushes the parameter inside the `value !== undefined` guard. `whereOr`'s `'in'` and `'contains'` branches today emit the clause but skip the push; `'ieq'`/`'ine'` aren't branches at all — they fall into the `else` where `operators['ieq']` is undefined, so the guard rejects them.

## Baseline notes

- `node_modules` was missing on this worktree. Ran `pnpm install`; lockfile resolution was already up to date.
- Previous-session testament `2026-05-11.md` exists untracked in `.claude/testament/`. Left as-is.

# 04:02

## Phase 1 rewrite — tdd skill applied

Supervisor blocked. The previous prompt direction "use the existing file's assertion style" lowered the bar below the `tdd` skill; that direction was removed.

Rewrote all 11 new `whereOr` tests to the expected/actual pattern:

```ts
it('...', () => {
  const expected = ...;

  const builder = createCosmosQueryBuilder<MyEntity>();
  // exercise
  const actual = builder.query().query; // or .parameters?.[0]

  expect(actual).toBe(expected); // toEqual for objects
});
```

Existing tests in the file are untouched — the skill applies to new code only.

## What didn't change

- Test count: 11 new tests, identical bug coverage to the first cast.
- Red shape: 9 of 11 fail with the same assertion errors as before (`undefined` vs `{name,value}` for parameter binding; missing `WHERE` clause for `ieq`/`ine`; `@p0` collision for compound cases). The two single-condition query-text tests for `in` and `contains` still pass because the clause text is what gets emitted on the happy path — the bug for those operators surfaces in the companion parameter-binding test and the compound case.
- Field choice (`givenName`/`familyName`), mixed-operator order (`in, eq, contains, ieq`), structural `toEqual` on parameter object — all kept.

## Why the structural `toEqual` is one assertion

The `tdd` skill says "one assertion per `it` block" for diagnostic clarity. `expect(actual).toEqual({ name, value })` is one structural assertion: a failing diff shows both fields together, which is still clear. Splitting into separate `name` and `value` tests would change the test count, which the supervisor explicitly said stays fixed.

# 05:46

## Phase 2 — Builder (whereOr Green)

Fixed `whereOr` in `packages/cosmos-query-builder/src/private/CosmosQueryBuilder.ts`. Mirrored the four `where` branches into `whereOr`:

- `'contains'` — wrapped the clause emission in `if (value !== undefined)` and added `this._parameters.push({ name: parameterName, value })` inside the guard.
- `'in'` — same treatment.
- `'ieq'` — new explicit branch emitting `StringEquals(c.${field}, ${parameterName}, true)` with the parameter push, guarded by `value !== undefined`.
- `'ine'` — new explicit branch emitting `Not(StringEquals(c.${field}, ${parameterName}, true))` with the parameter push, guarded the same way.

The `else` fallback and the `'isNull'` branch were left as-is. `'isNull'` doesn't push a parameter (intentional — no value); the fallback already pushed correctly.

## Verify

- `pnpm --filter @shellicar/cosmos-query-builder type-check` — pass.
- `pnpm --filter @shellicar/cosmos-query-builder test` — 20/20 pass (9 pre-existing + 11 from Phase 1, all green).

## Changes entry

Appended one `fixed` line to `packages/cosmos-query-builder/changes.jsonl`:

```
{"description":"Fix `whereOr` silently dropping `in`, `contains`, `ieq`, and `ine` conditions","category":"fixed"}
```

End of file. No release marker — that's Phase 3's job.

## Notes for Phase 3

Phase 3 is `github-version` Phase 3b on `cosmos-query-builder`. Brief specifies **patch** bump (currently 1.1.3 → 1.1.4). One `fixed` entry pending in `changes.jsonl`; patch is consistent with the change category.

## What didn't change

Public API surface untouched — no signature, type, or export changes. Only the runtime path inside the existing `whereOr` method body.
