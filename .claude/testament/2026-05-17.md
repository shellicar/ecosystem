# 03:33

## pnpm audit --fix writes to pnpm-workspace.yaml, not package.json

Despite the script's output saying "added N overrides to package.json", all overrides ended up in `pnpm-workspace.yaml`. This is consistent with how pnpm audit --fix works in this monorepo. Existing overrides in that file make it the target.

## protobufjs 8.x cluster emerged after first fix round

The initial 7 CVEs were fixed, but upgrading @opentelemetry/sdk-node to 0.217.0 brought in @opentelemetry/otlp-transformer@0.217.0 which depends on protobufjs@8.0.1 (vulnerable: >=8.0.0 <=8.0.1, fixed at >=8.0.2). Seven GHSAs cover this one package. The existing workspace override `protobufjs@<7.5.5: '>=7.5.5'` does NOT cover the 8.x range — the ranges are separate. Had to add `'protobufjs@>=8.0.0 <=8.0.1': '>=8.0.2'` manually (fix-ghsa.mjs failed: no semver package in the script's resolution context).

## fix-ghsa.mjs fails in this context

The script at `~/.claude/skills/maintenance-release-fleet/scripts/fix-ghsa.mjs` imports `semver` but can't find it when run from the repo root via `node`. It expects semver to be installed somewhere in the resolution chain. If this script is needed again, either install semver globally or add the override manually to pnpm-workspace.yaml (quick and clean).

## pnpm why -r surfaces published package exposure, audit paths do not

`pnpm audit` showed all CVEs under example paths only. `pnpm why -r` revealed:
- **devalue + svelte CVEs** also reach `@shellicar/svelte-adapter-azure-functions` via `@sveltejs/kit` in devDependencies. Dev-time only, not consumer-facing.
- **opentelemetry + protobufjs CVEs** also reach `@shellicar/winston-azure-application-insights` via `applicationinsightsv3: npm:applicationinsights@^3` in optionalDependencies. Consumer-facing: users who install optional deps would get the vulnerable chain.

This is why `pnpm why -r` is essential — audit paths show one path, not all.

## ncu updated version-specific pinned examples — requires manual revert

The reject list only included the alias name `applicationinsightsv2`, not the bare `applicationinsights` name used in the version-specific examples. ncu updated:
- `examples/cosmos-query-builder/cosmos451/package.json`: `@azure/cosmos 4.5.1 → 4.9.3` (directory name encodes the version)
- `examples/cosmos-query-builder/cosmos460/package.json`: `@azure/cosmos 4.6.0 → 4.9.3`
- `examples/winston-azure-application-insights/v2/package.json`: `applicationinsights ^2.9.6 → ^3.15.0` (major)
- `examples/winston-azure-application-insights/v3_03/package.json`: `applicationinsights 3.3.0 → 3.15.0`
- `examples/winston-azure-application-insights/v3_09/package.json`: `applicationinsights 3.9.0 → 3.15.0`
- `examples/winston-azure-application-insights/v3_12/package.json`: `applicationinsights 3.12.1 → 3.15.0`

These were reverted with EditFile (git restore/checkout are blocked). Next time: add `applicationinsights` to the reject list, or explicitly name these pinned packages.

## Workflow fix already done

The mission's additional task — remove `pnpm audit` from `.github/workflows/npm-publish.yml` — was already done in commit `8e10b7a` (PR #33, May 9 2026). No action needed.

## Audit JSON location

`.claude/audit/2026-05-16.json` — captured before fix-audit.sh ran. References the original 7 CVEs (opentelemetry, devalue, svelte). The protobufjs cluster appeared only after the first fix round upgraded opentelemetry to 0.217.0.

## Published package CVE summary for this release

| Package | CVEs | Exposure |
|---------|------|----------|
| @shellicar/winston-azure-application-insights | GHSA-q7rr-3cgh-j5r3 (opentelemetry), GHSA-66ff-xgx4-vchm, GHSA-75px-5xx7-5xc7, GHSA-jvwf-75h9-cwgg, GHSA-685m-2w69-288q, GHSA-2pr8-phx7-x9h3, GHSA-fx83-v9x8-x52w, GHSA-q6x5-8v7m-xcrf (protobufjs) | optionalDependencies, consumer-facing |
| @shellicar/svelte-adapter-azure-functions | GHSA-77vg-94rm-hx3p (devalue), GHSA-pr6f-5x2q-rwfp, GHSA-f3cj-j4f6-wq85, GHSA-rcqx-6q8c-2c42, GHSA-9rmh-mm8f-r9h6 (svelte) | devDependencies, dev-time only |

## Supervisor block and fix (03:50)

**Blocker:** `packages/core-di-lite/changes.jsonl` said `"Updated patch dependencies"` but the actual scope included a minor vitest bump (`^4.0.18 → ^4.1.6`, a 4.0→4.1 minor jump). The `maintenance-release-fleet` skill requires the description to match the actual scope. Fixed to `"Updated patch and minor dependencies"`.

**Root cause:** The debrief disclosed the minor deviation, but the entry was written as though the scope was patch-only. The two needed to agree.
