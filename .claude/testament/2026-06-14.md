# 22:36

## Phase 3 (Courier): pushed security/2026-06-14, opened PR #37

The esbuild + transitive-CVE security release. Branch pushed, PR [#37](https://github.com/shellicar/ecosystem/pull/37) open and `MERGEABLE`. `pnpm audit` is clean; build, test, and all examples were runtime-verified in Phases 1-2.

## CI trap: biome checks the committed audit JSON

`build (22.x)` runs `biome ci .` across the whole repo. The committed `.claude/audit/2026-06-14.json` (added in commit `1a5625f`) is not biome-formatted, so the build failed on it, not on the security work. Fix: excluded `.claude/audit/**` in `biome.json` `files.includes` (a single `!` excludes, matching the existing `!schema/*.json`). The audit JSON stays committed; biome just stops checking it. Local `biome ci` then checks 609 files with 0 errors.

## Testament lives ON THE BRANCH

This file belongs on the branch (`.claude/testament/2026-06-14.md` in the worktree), not the main checkout. Phases 1-2 wrote their entries to the main checkout (`~/repos/@shellicar/ecosystem`) by mistake; the SC corrected that here. Their verbose originals remain in the main checkout; this is the carried-forward version.

## What ships (Phase 4 releases)

Five packages bump, all **patch**. Tag format is `package-name@version`, no scope and no `v` prefix:

- `build-azure-local-settings@1.0.4`
- `build-clean@1.3.6`
- `build-version@1.3.10`
- `build-graphql@1.4.7`
- `svelte-adapter-azure-functions@1.0.7`

**winston-azure-application-insights does NOT release.** Its CVE fixes (grpc x2, uuid) were root `pnpm-workspace.yaml` overrides only, so its own published bytes did not change. The release test is **own-content**: a package releases only if its own `package.json` or source changed, not because a root override happened to fix a transitive dep. The five above changed `package.json` (esbuild declared range/peer); winston did not.

Phase 4: merge PR #37 first, then create a GitHub release per package in tier order (`maintenance-release-fleet` Phase 6); each release triggers the npm-publish workflow.

## What the branch fixes

Six advisories, all cleared:

- **esbuild** high `GHSA-gv7w-rqvm-qjhr` + low `GHSA-g7r4-m6w7-qqqr` -> `>=0.28.1`. Root override, plus declared range `^0.27.7 -> ^0.28.1` in the five packages and peer `^0.27 -> ^0.28` in the four peer-declarers.
- **shell-quote** critical `GHSA-w7jw-789q-3m8p`, **@grpc/grpc-js** high x2 (`GHSA-5375-pq7m-f5r2`, `GHSA-99f4-grh7-6pcq`), **uuid** moderate `GHSA-w5hq-g745-h8pq`: root overrides only (transitive dev or optional-dep chains).

One **non-security** commit `b99d62d` declares `undici ^6` for `applicationinsights@3.3.0` via `packageExtensions`. AI 3.3.0 eagerly `require`s undici but never declares it (a phantom dep that crashed the v3.3 example at load). Touches only `pnpm-workspace.yaml` + lockfile, no published package.

## Traps that cost time

- **uuid floors `>=11.1.1` but RESOLVES TO 14.0.0** (a six-major jump from 8.3.2) inside applicationinsights' old `@azure/functions`. Build, test, and examples pass, but the examples do not exercise the `@azure/functions` runtime uuid path, so runtime risk is low-but-not-zero. If a uuid break ever surfaces in the winston/AI path, this override is the suspect.
- **Changelog generator needs an ABSOLUTE path**: `pnpm --filter scripts changelog <abs>/packages/<pkg>`. The documented relative form resolves against cwd=`scripts/` and ENOENTs, writing nothing (so it is safe to re-run).
- **The brief's premise was wrong**: esbuild is not examples-only, it is declared across the five published packages. The fresh `pnpm audit` + `pnpm why -r` is authoritative; the brief's context note is not.

## SC decisions at the gate (Phase 1)

- **Scenario A**: security fixes only, no minor/major dep updates folded in.
- esbuild fixed via override + range bump + peer widen (not override-only). Peer replaced `^0.27 -> ^0.28` (not a union).
- All five bumps **patch**, governed by the in-repo precedent (vite peer `^6 -> ^7` shipped as a patch).
